{
  "name": "0agent-telegram-gateway",
  "version": "1.0.0",
  "description": "Multi-tenant Telegram bot — spins up a Docker container per user",
  "type": "module",
  "main": "bot.js",
  "scripts": {
    "start": "node bot.js",
    "dev": "node --watch bot.js"
  },
  "dependencies": {
    "node-telegram-bot-api": "^0.66.0",
    "dockerode": "^4.0.2",
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
