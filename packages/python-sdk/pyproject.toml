[project]
name = "zero-agent"
version = "0.1.0"
description = "Python SDK for 0agent"
requires-python = ">=3.10"
dependencies = ["httpx>=0.27.0", "pydantic>=2.6.0", "websockets>=12.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.backends"
